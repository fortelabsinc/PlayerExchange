---
- name: What is going on here?
  debug:
    msg: "gcr.io/forte-player-exchange/plyxchg:{{build_ver}}"

- name: Define the Deployment
  k8s:
    state: "{{cluster_state}}"
    wait: yes
    definition:
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: plyxchg-deployment
        namespace: "{{cluster_name}}-{{stage}}"
      spec:
        replicas: 3
        selector:
          matchLabels:
            app: plyxchg
        template:
          metadata:
            labels:
              app: plyxchg
          spec:
            containers:
              - name: plyxchg
                image: "gcr.io/forte-player-exchange/plyxchg:{{build_ver}}"
                resources:
                  limits:
                    memory: "2Gi"
                    cpu: "1000m"
                  requests:
                    memory: "1Gi"
                    cpu: "500m"
                livenessProbe:
                  httpGet:
                    path: /node/v1/healthy
                    port: 8182
                  initialDelaySeconds: 3
                  periodSeconds: 3
                ports:
                  - containerPort: 8180
