defmodule BlockchainTest do
  use ExUnit.Case
  require Logger
  doctest Blockchain

  test "greets the world" do
    # out = Blockchain.Ripple.PayID.lookup("mbhandary$forte.playerexchange.io")
    # Logger.info("#{inspect(out)}")
    # Blockchain.Ripple.XRP.health()
    # client = Blockchain.Ripple.client()
    # Blockchain.Ripple.health(client)
    # Blockchain.Ripple.create(client)
  end
end

# rippler.default.svc.cluster.local:3000
