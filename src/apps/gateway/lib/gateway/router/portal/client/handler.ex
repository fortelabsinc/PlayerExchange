defmodule Gateway.Router.Portal.Client.Handler do
  @moduledoc """
  Nothing to do here for the client because it is a static file
  display system only.  In the future we might need more, etc
  """
end
